.about {
  display: flex;
  flex-direction: row;
  height: 72px;
  align-items: center;
}

.logo {
  padding-left: 0;
  padding-right: 14px;
}

.info {
}

.name {
  font-size: 18px;
}

.version {
  font-size: 16px;
  color: #79838d;
}
